<template name="root">
  <block wx:if="{{node}}" wx:for="{{node.children}}" wx:key="uid">
    <template is="{{item.template}}" data="{{node: item}}" />
  </block>
</template>

<template name="tpl_view_0">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </view>
</template>
<!-- start tpl_view_0 默认展开5层 -->
<!-- children.length == 0 -->
<template name="tpl_view_0_c0">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}"></view>
</template>
<!-- children.length == 1 -->
<template name="tpl_view_0_c1">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
  </view>
</template>
<!-- children.length == 2 -->
<template name="tpl_view_0_c2">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
  </view>
</template>
<!-- children.length == 3 -->
<template name="tpl_view_0_c3">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
  </view>
</template>
<!-- children.length == 4 -->
<template name="tpl_view_0_c4">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
    <template is="{{node.children[3].template}}" data="{{node: node.children[3]}}" />
  </view>
</template>
<!-- end tpl_view_0 默认展开5层 -->

<template name="tpl_view_1">
  <view class="{{node.props.class || node.props.className}}" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </view>
</template>

<!-- start tpl_view_1 默认展开5层 -->
<!-- children.length == 0 -->
<template name="tpl_view_1_c0">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}"></view>
</template>
<!-- children.length == 1 -->
<template name="tpl_view_1_c1">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
  </view>
</template>
<!-- children.length == 2 -->
<template name="tpl_view_1_c2">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
  </view>
</template>
<!-- children.length == 3 -->
<template name="tpl_view_1_c3">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
  </view>
</template>
<!-- children.length == 4 -->
<template name="tpl_view_1_c4">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
    <template is="{{node.children[3].template}}" data="{{node: node.children[3]}}" />
  </view>
</template>
<!-- end tpl_view_1 默认展开5层 -->

<template name="tpl_view_2">
  <view class="{{node.props.class || node.props.className}}" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </view>
</template>
<!-- start tpl_view_2 默认展开5层 -->
<!-- children.length == 0 -->
<template name="tpl_view_2_c0">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}"></view>
</template>
<!-- children.length == 1 -->
<template name="tpl_view_2_c1">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
  </view>
</template>
<!-- children.length == 2 -->
<template name="tpl_view_2_c2">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
  </view>
</template>
<!-- children.length == 3 -->
<template name="tpl_view_2_c3">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
  </view>
</template>
<!-- children.length == 4 -->
<template name="tpl_view_2_c4">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
    <template is="{{node.children[3].template}}" data="{{node: node.children[3]}}" />
  </view>
</template>
<!-- end tpl_view_2 默认展开5层 -->

<template name="tpl_view_3">
  <view class="{{node.props.class || node.props.className}}" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </view>
</template>
<!-- start tpl_view_3 默认展开5层 -->
<!-- children.length == 0 -->
<template name="tpl_view_3_c0">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}"></view>
</template>
<!-- children.length == 1 -->
<template name="tpl_view_3_c1">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
  </view>
</template>
<!-- children.length == 2 -->
<template name="tpl_view_3_c2">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
  </view>
</template>
<!-- children.length == 3 -->
<template name="tpl_view_3_c3">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
  </view>
</template>
<!-- children.length == 4 -->
<template name="tpl_view_3_c4">
  <view class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <template is="{{node.children[0].template}}" data="{{node: node.children[0]}}" />
    <template is="{{node.children[1].template}}" data="{{node: node.children[1]}}" />
    <template is="{{node.children[2].template}}" data="{{node: node.children[2]}}" />
    <template is="{{node.children[3].template}}" data="{{node: node.children[3]}}" />
  </view>
</template>
<!-- end tpl_view_3 默认展开5层 -->

<template name="tpl_text_0">
  <text>{{ node.text }}</text>
</template>
<template name="tpl_text_1">
  <text>{{ node.text }}</text>
</template>

<template name="tpl_button_0">
  <button class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </button>
</template>
<template name="tpl_button_1">
  <button class="{{node.props.class || node.props.className}}" bindtap="$bindtap" id="{{node.uid}}">
    <block wx:for="{{node.children}}" wx:key="uid">
      <template is="{{item.template}}" data="{{node: item}}" />
    </block>
  </button>
</template>